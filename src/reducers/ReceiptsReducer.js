
const INITIAL_STATE = {
  isFetching: false, //current trip in progress?
  receiptsArr: []
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
